name: AI Code Review

on:
  push:
    branches:
      - '**'
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  ai_review:
    name: AI Code Review with OpenAI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Run AI Code Review
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }} # Store the OpenAI API key in secrets
        run: |
          pip install openai
          python -c """
          import openai
          import os

          openai.api_key = '${{ secrets.OPENAI_API_KEY }}'
          repo_path = '.'
          files_to_review = []
          for root, _, files in os.walk(repo_path):
              for file in files:
                  filepath = os.path.join(root, file)
                  with open(filepath, 'r', encoding='utf-8', errors='ignore') as f:
                      try:
                          files_to_review.append({'filename': filepath, 'content': f.read()})
                      except Exception as e:
                          files_to_review.append({'filename': filepath, 'content': f'Error reading file: {e}'})

          messages = [{'role': 'system', 'content': 'You are an AI specialized in code reviews.'}]
          for file in files_to_review:
               name = "Review this file" + file['filename']
               contents = file['content']
               file_info = name + " " + contents
              messages.append({'role': 'user', 'content': file_info})

          response = openai.ChatCompletion.create(
              model='gpt-4',
              messages=messages
          )
          print(response['choices'][0]['message']['content'])
          """